import { h, Component } from 'preact'
import style from './style'

export default class Dialog extends Component {
  render ({dialogItem}) {
    // console.log('render dialogItem', dialogItem)

    return (
      <div>
        Dialog
      </div>
    )

  }
}
        // {dialogItem && dialogItem}
